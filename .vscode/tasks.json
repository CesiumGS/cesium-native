{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Debug",
            "type": "shell",
            "command": "cmake -B build-debug -D CMAKE_BUILD_TYPE=Debug -S . && cmake --build build-debug --parallel 8",
            "group": "build",
            "problemMatcher": "$gcc"
        }
    ]
}