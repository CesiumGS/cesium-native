// This file was generated by generate-classes.
// DO NOT EDIT THIS FILE!
#pragma once

#include <CesiumGltf/Alias.h>
#include <CesiumGltf/Library.h>
#include <CesiumGltf/NamedObject.h>

#include <cstdint>
#include <optional>
#include <string>
#include <vector>

namespace CesiumGltf {
/**
 * @brief A reference to an external file. Files **MAY** be referenced by a URI
 * (or IRI) or a buffer view index.
 */
struct CESIUMGLTF_API File final : public CesiumGltf::NamedObject {
  /**
   * @brief The original name of this type.
   */
  static constexpr const char* TypeName = "File";

  /**
   * @brief Known values for The media type or MIME type of the external file.
   */
  struct MimeType {
    /** @brief `model/gltf+json` */
    inline static const std::string model_gltf_json = "model/gltf+json";

    /** @brief `model/gltf-binary` */
    inline static const std::string model_gltf_binary = "model/gltf-binary";
  };

  /**
   * @brief An array of aliases used for files within this external file.
   * Aliases allow overriding inner file URIs.
   */
  std::vector<CesiumGltf::Alias> aliases;

  /**
   * @brief The index of the bufferView that contains the file data.
   *
   * This field **MUST NOT** be defined when `uri` is defined.
   */
  int32_t bufferView = -1;

  /**
   * @brief The media type or MIME type of the external file.
   *
   * Known values are defined in @ref MimeType.
   *
   *
   * This field is required and **MUST** always be defined.
   */
  std::string mimeType = MimeType::model_gltf_json;

  /**
   * @brief The URI (or IRI) of the external file.
   *
   * Relative paths are relative to the current glTF asset.  Instead of
   * referencing an external file, this field **MAY** contain a `data:`-URI.
   * This field **MUST NOT** be defined when `bufferView` is defined.
   */
  std::optional<std::string> uri;

  /**
   * @brief Calculates the size in bytes of this object, including the contents
   * of all collections, pointers, and strings. This will NOT include the size
   * of any extensions attached to the object. Calling this method may be slow
   * as it requires traversing the object's entire structure.
   */
  int64_t getSizeBytes() const {
    int64_t accum = 0;
    accum += int64_t(sizeof(File));
    accum += CesiumGltf::NamedObject::getSizeBytes() -
             int64_t(sizeof(CesiumGltf::NamedObject));
    accum += int64_t(sizeof(CesiumGltf::Alias) * this->aliases.capacity());
    for (const CesiumGltf::Alias& value : this->aliases) {
      accum += value.getSizeBytes() - int64_t(sizeof(CesiumGltf::Alias));
    }
    if (this->uri) {
      accum += int64_t(this->uri->capacity() * sizeof(char));
    }
    return accum;
  }
};
} // namespace CesiumGltf
