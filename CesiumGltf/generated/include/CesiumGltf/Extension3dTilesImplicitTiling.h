// This file was generated by generate-classes.
// DO NOT EDIT THIS FILE!
#pragma once

#include <CesiumGltf/Library.h>
#include <CesiumUtility/ExtensibleObject.h>

#include <cstdint>
#include <string>

namespace CesiumGltf {
/**
 * @brief Implicit tiling defines a concise representation of quadtrees and
 * octrees in 3D Tiles.
 */
struct CESIUMGLTF_API Extension3dTilesImplicitTiling final
    : public CesiumUtility::ExtensibleObject {
  /**
   * @brief The original name of this type.
   */
  static constexpr const char* TypeName = "Extension3dTilesImplicitTiling";
  /** @brief The official name of the extension. This should be the same as its
   * key in the `extensions` object. */
  static constexpr const char* ExtensionName = "3DTILES_implicit_tiling";

  /**
   * @brief Known values for Either QUADTREE or OCTREE. See Subdivision scheme.
   */
  struct SubdivisionScheme {
    /** @brief `QUADTREE` */
    inline static const std::string QUADTREE = "QUADTREE";

    /** @brief `OCTREE` */
    inline static const std::string OCTREE = "OCTREE";
  };

  /**
   * @brief Template URI for locating content files.
   *
   * A template URI pointing to tile content. It contains placeholders `{level}`, `{x}`, `{y}`, and optionally `{z}` (for OCTREE subdivision scheme) that are substituted with a tile's coordinate to resolve the actual file. Example: `content/{level}/{x}/{y}.glb`.
   */
  std::string contentUri;

  /**
   * @brief Template URI for locating subtree files.
   *
   * A template URI pointing to subtree files. It contains placeholders `{level}`, `{x}`, `{y}`, and optionally `{z}` (for OCTREE subdivision scheme) that are substituted with a subtree's coordinate to resolve the actual file. Example: `subtrees/{level}/{x}/{y}.json`.
   */
  std::string subtreeUri;

  /**
   * @brief Either QUADTREE or OCTREE. See Subdivision scheme.
   *
   * Known values are defined in @ref SubdivisionScheme.
   *
   */
  std::string subdivisionScheme = SubdivisionScheme::QUADTREE;

  /**
   * @brief How many levels there are in the tree with available tiles.
   */
  int64_t availableLevels = int64_t();

  /**
   * @brief How many levels there are in each subtree.
   */
  int64_t subtreeLevels = int64_t();

  /**
   * @brief Calculates the size in bytes of this object, including the contents
   * of all collections, pointers, and strings. This will NOT include the size
   * of any extensions attached to the object. Calling this method may be slow
   * as it requires traversing the object's entire structure.
   */
  int64_t getSizeBytes() const {
    int64_t accum = 0;
    accum += int64_t(sizeof(Extension3dTilesImplicitTiling));
    accum += CesiumUtility::ExtensibleObject::getSizeBytes() -
             int64_t(sizeof(CesiumUtility::ExtensibleObject));
    accum += int64_t(this->contentUri.capacity() * sizeof(char));
    accum += int64_t(this->subtreeUri.capacity() * sizeof(char));
    return accum;
  }
};
} // namespace CesiumGltf
