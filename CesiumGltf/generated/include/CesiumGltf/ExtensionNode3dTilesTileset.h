// This file was generated by generate-classes.
// DO NOT EDIT THIS FILE!
#pragma once

#include <CesiumGltf/Content.h>
#include <CesiumGltf/Library.h>
#include <CesiumUtility/ExtensibleObject.h>

#include <optional>
#include <string>

namespace CesiumGltf {
/**
 * @brief Node-level 3DTILES_tileset extension.
 */
struct CESIUMGLTF_API ExtensionNode3dTilesTileset final
    : public CesiumUtility::ExtensibleObject {
  /**
   * @brief The original name of this type.
   */
  static constexpr const char* TypeName = "ExtensionNode3dTilesTileset";
  /** @brief The official name of the extension. This should be the same as its
   * key in the `extensions` object. */
  static constexpr const char* ExtensionName = "3DTILES_tileset";

  /**
   * @brief Known values for Refinement determines the process by which a lower
   * resolution parent tile renders when its higher resolution children are
   * selected.
   */
  struct Refine {
    /** @brief `REPLACE` */
    inline static const std::string REPLACE = "REPLACE";

    /** @brief `ADD` */
    inline static const std::string ADD = "ADD";
  };

  /**
   * @brief A nonnegative number that defines the error, in meters, introduced
   * if this tile is rendered and its children are not.
   *
   * The tile geometric error. A nonnegative number that defines the error, in
   * meters, introduced if this tile is rendered and its children are not. At
   * runtime, the geometric error is used to compute Screen-Space Error (SSE),
   * the error measured in pixels. The SSE determines if a tile is sufficiently
   * detailed for the current view or if its children should be considered.
   */
  double geometricError = double();

  /**
   * @brief Refinement determines the process by which a lower resolution parent
   * tile renders when its higher resolution children are selected.
   *
   * Known values are defined in @ref Refine.
   *
   *
   * Specifies the refinement type. A string that is either "REPLACE" for
   * replacement refinement or "ADD" for additive refinement. It is required for
   * the root tile of a tileset; it is optional for all other tiles. A tileset
   * can use any combination of additive and replacement refinement. When the
   * refine property is omitted, it is inherited from the parent tile.
   */
  std::optional<std::string> refine;

  /**
   * @brief An object that provides additional information about the tile's
   * content. If the tile does not have content (i.e. the node's `externalAsset`
   * property is not defined) then `content` **MUST** be omitted.
   */
  std::optional<CesiumGltf::Content> content;

  /**
   * @brief Calculates the size in bytes of this object, including the contents
   * of all collections, pointers, and strings. This will NOT include the size
   * of any extensions attached to the object. Calling this method may be slow
   * as it requires traversing the object's entire structure.
   */
  int64_t getSizeBytes() const {
    int64_t accum = 0;
    accum += int64_t(sizeof(ExtensionNode3dTilesTileset));
    accum += CesiumUtility::ExtensibleObject::getSizeBytes() -
             int64_t(sizeof(CesiumUtility::ExtensibleObject));
    if (this->content) {
      accum +=
          this->content->getSizeBytes() - int64_t(sizeof(CesiumGltf::Content));
    }
    return accum;
  }
};
} // namespace CesiumGltf
