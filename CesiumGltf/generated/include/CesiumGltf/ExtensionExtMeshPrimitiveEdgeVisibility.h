// This file was generated by generate-classes.
// DO NOT EDIT THIS FILE!
#pragma once

#include <CesiumGltf/Library.h>
#include <CesiumGltf/LineString.h>
#include <CesiumUtility/ExtensibleObject.h>

#include <cstdint>
#include <vector>

namespace CesiumGltf {
/**
 * @brief glTF extension encoding the visibility of the edges of a triangles
 * primitive
 */
struct CESIUMGLTF_API ExtensionExtMeshPrimitiveEdgeVisibility final
    : public CesiumUtility::ExtensibleObject {
  /**
   * @brief The original name of this type.
   */
  static constexpr const char* TypeName =
      "ExtensionExtMeshPrimitiveEdgeVisibility";
  /** @brief The official name of the extension. This should be the same as its
   * key in the `extensions` object. */
  static constexpr const char* ExtensionName =
      "EXT_mesh_primitive_edge_visibility";

  /**
   * @brief The index of the accessor encoding the visibility of each triangle
   * edge as a bitfield with two bits per edge.
   *
   * An edge visibility of 0 indicates the edge is never drawn; 1 indicates the
   * edge is only drawn in silhouette; 2 indicates a 'hard' (always-drawn) edge;
   * and 3 indicates a repeated occurrence of a 'hard' edge.
   */
  int32_t visibility = -1;

  /**
   * @brief The index of a glTF material with which the edges are to be drawn.
   *
   * If omitted, the edges are drawn using the same material as the triangle
   * mesh primitive.
   */
  int32_t material = -1;

  /**
   * @brief The index of the accessor encoding the pair of outward-facing
   * normals for the two faces sharing each silhouette edge (visibility 1)
   * encoded in `visibility`.
   *
   * This property **MUST** be defined if and only if `visibility` encodes at
   * least one edge with visibility value `1`.
   */
  int32_t silhouetteNormals = -1;

  /**
   * @brief An array of edges encoded as line strings.
   *
   * Edges encoded as line strings **MUST NOT** also be encoded by the
   * `visibility` property.
   */
  std::vector<CesiumGltf::LineString> lineStrings;

  /**
   * @brief Calculates the size in bytes of this object, including the contents
   * of all collections, pointers, and strings. This will NOT include the size
   * of any extensions attached to the object. Calling this method may be slow
   * as it requires traversing the object's entire structure.
   */
  int64_t getSizeBytes() const {
    int64_t accum = 0;
    accum += int64_t(sizeof(ExtensionExtMeshPrimitiveEdgeVisibility));
    accum += CesiumUtility::ExtensibleObject::getSizeBytes() -
             int64_t(sizeof(CesiumUtility::ExtensibleObject));
    accum +=
        int64_t(sizeof(CesiumGltf::LineString) * this->lineStrings.capacity());
    for (const CesiumGltf::LineString& value : this->lineStrings) {
      accum += value.getSizeBytes() - int64_t(sizeof(CesiumGltf::LineString));
    }
    return accum;
  }
};
} // namespace CesiumGltf
