// This file was generated by generate-classes.
// DO NOT EDIT THIS FILE!
#pragma once

#include <CesiumGltf/Library.h>
#include <CesiumUtility/ExtensibleObject.h>

#include <string>
#include <vector>

namespace CesiumGltf {
/**
 * @brief KHR_billboard glTF Node Extension
 */
struct CESIUMGLTF_API ExtensionKhrBillboard final
    : public CesiumUtility::ExtensibleObject {
  /**
   * @brief The original name of this type.
   */
  static constexpr const char* TypeName = "ExtensionKhrBillboard";
  /** @brief The official name of the extension. This should be the same as its
   * key in the `extensions` object. */
  static constexpr const char* ExtensionName = "KHR_billboard";

  /**
   * @brief Known values for rotationAxis
   */
  struct RotationAxisItems {
    /** @brief `X` */
    inline static const std::string X = "X";

    /** @brief `Y` */
    inline static const std::string Y = "Y";

    /** @brief `Z` */
    inline static const std::string Z = "Z";
  };

  /**
   * @brief If true, the billboard is rendered on top of other objects.
   * (Default: false)
   */
  bool overlay = false;

  /**
   * @brief The axes around which the billboard rotates. 'X', 'Y', and/or 'Z'.
   * (Default: ['X', 'Y', 'Z'])
   */
  std::vector<std::string> rotationAxis = {"X", "Y", "Z"};

  /**
   * @brief If true, the billboard scales with distance from the camera.
   * (Default: false)
   */
  bool scaleWithDistance = false;

  /**
   * @brief The up direction of the billboard. Must be defined if
   * `viewDirection` is parallel to the default up vector. (Default: +Y)
   */
  std::vector<double> up = {0, 1, 0};

  /**
   * @brief The view direction of the billboard. (Default: +Z)
   */
  std::vector<double> viewDirection = {0, 0, 1};

  /**
   * @brief Calculates the size in bytes of this object, including the contents
   * of all collections, pointers, and strings. This will NOT include the size
   * of any extensions attached to the object. Calling this method may be slow
   * as it requires traversing the object's entire structure.
   */
  int64_t getSizeBytes() const {
    int64_t accum = 0;
    accum += int64_t(sizeof(ExtensionKhrBillboard));
    accum += CesiumUtility::ExtensibleObject::getSizeBytes() -
             int64_t(sizeof(CesiumUtility::ExtensibleObject));
    accum += int64_t(sizeof(std::string) * this->rotationAxis.capacity());
    accum += int64_t(sizeof(double) * this->up.capacity());
    accum += int64_t(sizeof(double) * this->viewDirection.capacity());
    return accum;
  }
};
} // namespace CesiumGltf
